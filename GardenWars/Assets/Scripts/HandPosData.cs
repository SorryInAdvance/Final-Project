using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HandPosData : MonoBehaviour {
	[HideInInspector]
	public bool empty = false; //will have the infomation if card is in the current space or not

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
